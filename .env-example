# Exemplo de variáveis de ambiente para o script Pipefy
# Copie este arquivo para `.env` e preencha os valores reais.

# Autenticação (uma das duas opções abaixo):
# 1) Token direto (mais simples):
PIPEFY_TOKEN= COLOQUE SEU TOKEN DO PIPEFY AQUI

# OU 2) Client credentials (usado para obter token via OAuth client_credentials):
PIPEFY_CLIENT_ID=
PIPEFY_CLIENT_SECRET=

# ID do Pipe onde os cards serão criados (obrigatório)
PIPE_ID= COLOQUE SEU ID DO PIPE 

# Opcional: IDs dos campos no Pipe para popular os campos do card
# Se não fornecer, o script apenas criará um card com o título contendo Razão Social e CNPJ
PIPE_FIELD_RAZAO=raz_o_social
PIPE_FIELD_CNPJ=cnpj
PIPE_FIELD_PATRIMONIO=patrim_nio_l_quido

# URL do ZIP 
ZIP_URL=https://dados.cvm.gov.br/dados/FI/CAD/DADOS/registro_fundo_classe.zip
CSV_NAME=registro_fundo.csv

# Endpoint GraphQL do Pipefy
API_URL=https://api.pipefy.com/graphql
